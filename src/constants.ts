export enum CellState {
  DEAD = 'dead',
  ALIVE = 'alive',
}

export enum SpeedType {
  FAST = 'fast',
  MEDIUM = 'medium',
  SLOW = 'slow',
}

export enum FillType {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

export enum BoardSize {
  LARGE = 'large',
  MEDIUM = 'medium',
  SMALL = 'small',
}
